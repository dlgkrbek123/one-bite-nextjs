------------------------------------------------------------------------

기존 리액트에서는 useState, useEffect 조합으로 쿼리
state가 null이면 loading 리턴

단점 : 마운트 이후에나 요청이 진행되어 빈화면이 보여지는 기간이 길다.
- 안그래도 긴 FCP 이후에나 요청

------------------------------------------------------------------------

사전 렌더링전에 백엔드서버에서 필요한 데이터를 먼저 불러온다
- 빠른 타이밍에 요청 실행

------------------------------------------------------------------------

SSR
- 가징 기본적인 사전렌더링 방식
- 요청이 들어올때마다 사전 렌더링 수행

getServerSideProps를 정의
- props를 포함하는 객체를 리턴해야함
- 사전렌더링 과정에서 컴포넌트보다 먼저 실행

- 사전렌더링에서 서버측에서 딱 한번만 실행됨
- window에 접근 못해 (서버와 클라이언트에서 한번씩 실행되는걸 유념)

------------------------------------------------------------------------

SSG
- 빌드시간에 사전렌더링을 한번만 수행

------------------------------------------------------------------------

ISR
- 

------------------------------------------------------------------------

InferGetServerSidePropsType 내장타입을 이용하면 getServerSideProps로부터 페이지 
컴포넌트 props를 간단히 추론 가능

------------------------------------------------------------------------