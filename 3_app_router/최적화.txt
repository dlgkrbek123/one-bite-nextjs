------------------------------------------------------------------------

이미지가 웹에서 용량의 대다수를 차지
- 확장자를 경량화된 포맷으로 바꾸거나 (webp,avif)
- 디바이스 사이즈별 이미지 제공
- 레이지 로딩
- blur 이미지 활용...

Next에서 자체적으로 이미지 최적화를 제공 (<Iamge />)
- webp 적용
- lazy loading
- !! width, height 지정 필요 !!
- 외부서버의 이미지에는 적용 못함...
  - next.config.js

------------------------------------------------------------------------

page컴포넌트에서 metadata라는 변수를 export해서 SEO 정의
favicon은 app폴더 밑에있는거 교체하셈

export const metadata: Metadata = {
  title: "한입 북스",
  description: "한입 북스에 등록된 도서를 만나보세요",
  openGraph: {
    title: "한입 북스",
    description: "한입 북스에 등록된 도서를 만나보세요",
    images: ["/thumbnail.png"],
  },
};


export async function generateMetadata({
  searchParams,
}: {
  searchParams: Promise<{
    q?: string;
  }>;
}) {
  const { q = "" } = await searchParams;

  return {
    title: `${q} : 한입북스 검색`,
    description: "한입 북스에 등록된 도서를 만나보세요",
    openGraph: {
      title: `${q} : 한입북스 검색`,
      description: "한입 북스에 등록된 도서를 만나보세요",
      images: ["/thumbnail.png"],
    },
  };
}

------------------------------------------------------------------------
